version: "3.8"
services:
  application:
    build: ./application
    ports:
      - target: 5500
        published: 5500
  nginx:
    image: nginx:latest
    volumes:
    - type: bind
      source: ./nginx
      target: etc/nginx/nginx.conf
    ports:
      - target: 80
        published: 80
